import { Box } from "@mui/material";

const RewindButton = ({ player }) => {
  return (
    <Box id="button_rewind" component="img" src="/images/thumbs/controls/10-seconds-backward.svg" />
  );
};

export default RewindButton;
