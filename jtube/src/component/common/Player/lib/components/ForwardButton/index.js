import { Box } from "@mui/material";

const ForwardButton = ({ player }) => {
  return (
    <Box id="button_forward" component="img" src="/images/thumbs/controls/10-seconds-forward.svg" />
  );
};

export default ForwardButton;
