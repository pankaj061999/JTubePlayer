import { Box } from "@mui/material";

const NextButton = ({ player }) => {
  return <Box id="button_next" component="img" src="/images/thumbs/controls/next-button.svg" />;
};

export default NextButton;
